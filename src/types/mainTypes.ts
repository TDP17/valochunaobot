export type signupsListType = { username: string; rank: number; }[];

export type randomizedReturnType = {
    includedList: string[],
    excludedList: string[],
}

export type usernameListType = {
    username: string
}[];